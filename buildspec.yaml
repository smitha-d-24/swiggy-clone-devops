version: 0.2

env:
  parameter-store:
    DOCKER_USERNAME: "/cicd/docker-credentials/username"
    DOCKER_PASSWORD: "/cicd/docker-credentials/password"
    DOCKER_REGISTRY: "/cicd/docker-registry/url"

phases:
  install:
    runtime-versions:
      nodejs: 18
      java: corretto17
    commands:
      - echo "Installing dependencies"
      - npm install

      - echo "Installing Trivy"
      - wget https://github.com/aquasecurity/trivy/releases/download/v0.50.2/trivy_0.50.2_Linux-64bit.tar.gz
      - tar -xzf trivy_0.50.2_Linux-64bit.tar.gz
      - chmod +x trivy
      - mv trivy /usr/local/bin/

      - echo "Installing OWASP Dependency Check"
      - wget https://github.com/jeremylong/DependencyCheck/releases/download/v9.0.9/dependency-check-9.0.9-release.zip
      - unzip dependency-check-9.0.9-release.zip

  pre_build:
    commands:
      - echo "Running Trivy filesystem scan"
      - trivy fs --exit-code 0 --no-progress . > trivy-fs-report.txt

      - echo "Running OWASP Dependency Check"
      - ./dependency-check/bin/dependency-check.sh --scan . --format HTML --out .

      - echo "Logging into DockerHub"
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

  build:
    commands:
      - echo "Building Docker image"
      - docker build -t swiggy-clone .
      - docker tag swiggy-clone "$DOCKER_USERNAME/swiggy-clone:latest"

  post_build:
    commands:
      - echo "Pushing Docker image"
      - docker push "$DOCKER_USERNAME/swiggy-clone:latest"
      - printf '[{"name":"swiggy","imageUri":"%s"}]' "$DOCKER_USERNAME/swiggy-clone:latest" > imagedefinitions.json
      - echo "Build completed successfully"

artifacts:
  files:
    - appspec.yaml
    - imagedefinitions.json
    - trivy-fs-report.txt
    - dependency-check-report.html
